
```dataview
TABLE category AS "เครื่องมือ/ระบบ", description AS "คำอธิบายการทำงาน", usage AS "ความบ่อยในการใช้"

FROM #linux
SORT category ASC
```


```

### 🐧 รวมคำสั่ง Linux พื้นฐาน
```bash
# ตรวจสอบสิทธิ์การเข้าถึงไฟล์ (Permission)
ls -la

# ยกระดับสิทธิ์เป็น Root (Administrator)
sudo -i


```


